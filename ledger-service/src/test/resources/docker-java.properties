# Docker Engine 29+ requires client API >= 1.44; docker-java's default is older,
# so the daemon rejects the handshake with an empty 400. Remove once Testcontainers
# manages a docker-java that negotiates this itself (2.x).
api.version=1.44
